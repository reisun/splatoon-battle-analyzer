services:
  app:
    build: .
    volumes:
      - .:/app
      - ~/.claude.json:/home/appuser/.claude.json:ro
      - ~/.claude:/home/appuser/.claude:ro
    env_file:
      - .env
    working_dir: /app
    networks:
      - llm-network

networks:
  llm-network:
    external: true
